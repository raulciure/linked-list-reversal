#include "Linked-List.h"

/// @brief A function that reverses the linked list.
/// @param list The pointer to the first node of the list.
/// @param prevNode The pointer to the previous node of the list.
/// @return Pointer to the first node of the reversed list.
ListNode* listReverse(ListNode* list, ListNode* prevNode);